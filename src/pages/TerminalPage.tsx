import TerminalView from '../components/TerminalView'; // Adjust path if needed

const TerminalPage = () => {
  // This page's only job is to render the full-screen terminal.
  return <TerminalView />;
};

export default TerminalPage;
